import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const Color darkBlue = Color(0xFF071F4F); // Azul institucional escuro
  static const Color primary = Color(0xFF7C3AED); // Roxo ConeCTEA
  static const Color teal = Color(0xFF14B8A6); // Teal / Verde água
  static const Color statusGreen = Color(0xFF10B981); // Verde status ativo
  static const Color alertOrange = Color(0xFFF59E0B); // Laranja alerta / em análise
  static const Color errorRed = Color(0xFFEF4444); // Vermelho notificação
  static const Color cyan = Color(0xFF06B6D4); // Ciano
  static const Color cardBlue = Color(0xFF3B82F6); // Azul para cards

  // Cores de suporte
  static const Color backgroundLight = Color(0xFFF8FAFC); // Fundo geral
  static const Color backgroundPremium = Color(0xFFF6F8FC); // Fundo premium solicitado
  static const Color backgroundQuickAccess = Color(0xFFF3F6FF); // Fundo seção acesso rápido
  static const Color borderQuickAccess = Color(0xFFE0E7FF); // Borda seção acesso rápido
  static const Color whiteCard = Color(0xFFFFFFFF); // Branco dos cards
  static const Color textPrimary = Color(0xFF071F4F); // Texto principal (Azul escuro)
  static const Color textSecondary = Color(0xFF64748B); // Texto secundário (Cinza)
  
  // Cores de fundo suaves
  static const Color purpleLight = Color(0xFFEDE9FE); // Lilás claro para avatares/ícones
  static const Color tealLight = Color(0xFFF0FDFA); // Teal muito claro para status
  static const Color greenLight = Color(0xFFECFDF5); // Verde muito claro para status
  static const Color orangeLight = Color(0xFFFFF7ED); // Laranja muito claro para alerta
  
  // Cores Admin (correção de erros do terminal)
  static const Color successGreen = Color(0xFF10B981);
  static const Color alertYellow = Color(0xFFF59E0B);
  static const Color warning = Color(0xFFF59E0B);

  // Bordas e Sombras
  static const Color borderLight = Color(0xFFE5E7EB);
  static const Color shadowColor = Color(0xFF0B1F4D);

  // Novas cores de Botões ADM (Ciclo de Vida)
  static const Color adminPositive = Color(0xFF10B981);
  static const Color adminAnalysis = Color(0xFFF59E0B);
  static const Color adminRequest = Color(0xFF3B82F6);
  static const Color adminDanger = Color(0xFFEF4444);
  static const Color adminBlock = Color(0xFF1F2937);
}
